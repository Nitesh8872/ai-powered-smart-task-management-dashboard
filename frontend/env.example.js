// Optional. On Vercel you normally do NOT need this — the app uses /api via vercel.json.
// Only use for local testing against a remote Render API:
// window.APP_CONFIG = { API_BASE_URL: "https://your-render-backend.onrender.com/api" };
window.APP_CONFIG = {};
