NODE_ENV=development
PORT=5000
MONGO_URI=mongodb://127.0.0.1:27017/student-dashboard
JWT_SECRET=replace-with-a-long-random-secret
REFRESH_SECRET=replace-with-a-different-long-random-secret
# Optional. Production allows all origins by default (set STRICT_CORS=true to restrict).
# CORS_ORIGIN=*.vercel.app,http://localhost:5500
RATE_LIMIT_MAX=100
